@model IEnumerable<TailorManagementApp.Models.Staff>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-block">
                    <div id="placeHolder"></div>
                    <div class="col-lg-12">
                        <b><i class="ti-tag"></i> <span><b>Staff List</b></span></b>
                        <a class="btn btn-success" style="margin-bottom:12px;float:right;" asp-action="Create"><i class="ti-plus"></i>Create New</a>
                    </div>
                    <br />
                    <hr />
                    <div class="dt-responsive table-responsive">

                        <table class="table table-striped table-bordered nowrap" id="staffTable">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Name)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Phone)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Address)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.NID)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.RegisterDate)
                                    </th>

                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            <img src="@item.ImagePath"
                                                 class="rounded-circle"
                                                 height="40" width="40"
                                                 asp-append-version="true" />
                                            @item.Name
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Phone)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Address)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.NID)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.RegisterDate)
                                        </td>

                                        <td>
                                            <a asp-action="Edit" asp-route-id="@item.StaffID"><i class="ti-pencil"></i></a> |
                                            <a data-toggle="ajax-modal" id="delete" data-target="#customerModal" data-url="@Url.Action($"Details/{item.StaffID}")"><i class="ti-list"></i></a> |
                                            <a data-toggle="ajax-modal" id="delete" data-target="#customerModal" data-url="@Url.Action($"Delete/{item.StaffID}")"><i class="ti-trash"></i></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    </div></div></div></div>
                    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.22/css/jquery.dataTables.css">

                    @section scripts{

                        <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.10.22/js/jquery.dataTables.js"></script>
                        <script type="text/javascript" src="~/js/Popup.js"></script>
                        <script>

                            $(document).ready(function () {
                                $('#staffTable').DataTable({
                                    "paging": true,
                                    "ordering": true,
                                    "info": true
                                });
                            });


                            function PreviewImg(input) {
                                var File = input.files;

                                if (File && File[0]) {
                                    ReadImage(File[0]);
                                }
                            }

                        </script>

                    }

